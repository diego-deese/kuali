import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  requirementsHeader: {
    flexDirection: 'row',
    gap: 8,
    alignItems: 'center',
    marginBottom: 8,
  },
  subtitle: {
    fontFamily: 'monserratBold',
    fontSize: 16,
  },
})
